name: zivo
description: "ZIVO — a private single-user Personal OS."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  google_fonts: ^8.2.1
  image_picker: ^1.2.3
  path_provider: ^2.1.5
  # Cross-platform path joins/splits for the local media store's relative refs.
  path: ^1.9.0
  package_info_plus: ^8.3.1
  # Saves app-captured media into the device's system Photos/Gallery — the
  # opt-in "Save to Photos" media backup target. Well-maintained; the older
  # image_gallery_saver is abandoned.
  gal: ^2.3.0

  # Firebase Core, Auth, Functions, and now Firestore (user profiles). The
  # rest of the repository layer is still in-memory.
  firebase_core: ^4.1.1
  firebase_auth: ^6.5.7
  cloud_functions: ^6.0.3
  cloud_firestore: ^6.0.0
  google_sign_in: ^7.2.0
  sign_in_with_apple: ^8.1.0
  crypto: ^3.0.7
  firebase_app_check: ^0.4.6
  lottie: ^3.5.1
  # Premium, consistent line-icon set used app-wide via core/theme/app_icons.
  # (Lucide — Phosphor's package can't compile on this Flutter: it subclasses
  # the now-final IconData.)
  lucide_icons_flutter: ^3.0.0
  # Native crop/straighten/rotate editor for moment photos (iOS Photos-style
  # cropper; uCrop on Android).
  image_cropper: ^12.0.0
  # Google Drive backup target (Phase 2): the Drive v3 API plus a plain http
  # client we wrap with google_sign_in's OAuth bearer token (drive.file scope).
  googleapis: ^14.0.0
  http: ^1.2.0
  # Lets the media pipeline honor "Wi-Fi only" for automatic Drive backups.
  connectivity_plus: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Generates iOS/Android launcher icons from the ZIVO brand mark.
  flutter_launcher_icons: ^0.14.4

  # In-memory Firestore fake for testing FirestoreTaskRepository without a
  # real backend (or a FirebaseAuth mock — the repo takes a plain uid source).
  fake_cloud_firestore: ^4.0.0

# App identity — the ZIVO "Dark" mark (paper Z on near-black #101317).
# Regenerate after changing the source image with:
#   dart run flutter_launcher_icons
# Source art lives in assets/app-icon/ (see assets/README.txt). iOS uses the
# full-bleed square (the OS masks the corners); Android uses an adaptive icon
# (solid #101317 background + the same dark square as the foreground layer — the
# Z sits within the adaptive safe zone).
flutter_launcher_icons:
  ios: true
  android: true
  remove_alpha_ios: true
  image_path: "assets/app-icon/zivo-icon-dark-1024.png"
  min_sdk_android: 23
  adaptive_icon_background: "#101317"
  adaptive_icon_foreground: "assets/app-icon/zivo-icon-dark-1024.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # The branded loading animation, shown wherever a reactive surface is
  # waiting on its first stream value (see LoadingStateView).
  #
  # transparent/ is the wordmark's Z knocked out to alpha — the "paper"
  # (light) files are for ZIVO's dark surfaces, "ink" (dark) for light ones.
  assets:
    - assets/loading.json
    - assets/transparent/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
