<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Saving app-captured photos into the system gallery (gal / "Save to
         Photos"). API 29+ uses scoped storage (MediaStore) and needs no
         permission; only API 28 and below require this legacy write grant. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <!-- Ask's mic button — recording a voice note for speech-to-text
         (`record` package, `lib/features/ai/data/audio_recorder.dart`). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Today's step counter (pedometer / `lib/features/device/steps`).
         Android 10+ gates the step sensor behind this runtime permission. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <!-- Sleep (lib/features/sleep). READ ONLY: ZIVO never writes to Health
         Connect, so no WRITE_SLEEP. READ_HEALTH_DATA_HISTORY is what allows
         the 90-day backfill — without it Health Connect ERRORS on any read
         older than 30 days rather than returning less. Both are runtime
         permissions granted in Health Connect's own UI, not ours.
         Background reads (READ_HEALTH_DATA_IN_BACKGROUND) are deliberately
         not requested yet: nothing reads while backgrounded until the
         observer work lands (docs/SLEEP_SYSTEM.md §20), and asking for a
         permission we do not use is a permission we cannot justify. -->
    <uses-permission android:name="android.permission.health.READ_SLEEP" />
    <uses-permission android:name="android.permission.health.READ_HEALTH_DATA_HISTORY" />

    <!-- Local reminders (lib/features/reminders, flutter_local_notifications).
         POST_NOTIFICATIONS is the Android 13+ runtime grant, requested from the
         Reminders page. RECEIVE_BOOT_COMPLETED lets the boot receiver below
         re-arm scheduled reminders after a restart. Deliberately NO
         SCHEDULE_EXACT_ALARM: scheduling is inexact (AndroidScheduleMode.
         inexactAllowWhileIdle), so a meal/workout nudge needs no special-access
         grant. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Health Connect resolves this to decide which apps can request health
         permissions at all; on Android 13 and below it also needs the
         queries entry so the client can see the Health Connect APK. -->
    <queries>
        <package android:name="com.google.android.apps.healthdata" />
        <intent>
            <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
        </intent>
    </queries>

    <application
        android:label="zivo"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Health Connect's "why does this app want my data?" link, and
                 its Android 14+ framework equivalent. Both point at the app's
                 own launcher activity; Health Connect will not show the
                 permission sheet for an app that declares neither. -->
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
            <!-- Spotify redirect URI (lib/features/music) — App Remote's
                 actual Android auth flow is Activity-result based (handled
                 by spotify_sdk's plugin itself, no intent-filter needed for
                 that), but this is registered for parity with iOS and any
                 browser-based OAuth fallback that does deep-link back in. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="zivo" android:host="spotify-callback"/>
            </intent-filter>
        </activity>
        <!-- flutter_local_notifications: the receiver that posts a scheduled
             reminder, and the boot receiver that re-arms all scheduled
             reminders after a device restart or app update. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        <!-- image_cropper (uCrop) — the native Android crop/rotate activity. -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".MainActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
