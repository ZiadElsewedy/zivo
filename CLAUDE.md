# CLAUDE.md

> **Adapter file.** ZIVO's agent guidance is agent-neutral and lives in
> [`AGENTS.md`](AGENTS.md) + [`docs/`](docs). This file exists only so Claude Code
> auto-loads the pointer.

**→ Read [`AGENTS.md`](AGENTS.md) first.** It is the entry point for every AI agent
(Claude, Codex, Cursor, or any other): the boot/read order, the codebase map, the
constraints, and the docs index. Do not duplicate project knowledge into this file — keep
it here in the shared docs so every agent sees the same source of truth.

Quick links: [`docs/PRODUCT.md`](docs/PRODUCT.md) (what ZIVO is + what makes it different) ·
[`docs/STATE.md`](docs/STATE.md) (current state — read every session) · each
`lib/features/<x>/FEATURE.md` (per-feature map).
